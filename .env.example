# פורט השרת
PORT=3000

# מסד נתונים:
#   • אם מוגדר DATABASE_URL → משתמש ב-PostgreSQL (נתונים נשמרים לצמיתות, מומלץ לפרודקשן)
#   • אם ריק/לא מוגדר       → משתמש ב-SQLite מקומי (אפס-הגדרות, לפיתוח)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# סוד לחתימת טוקני התחברות (session). שנה לערך אקראי וארוך בפרודקשן!
SESSION_SECRET=change-me-to-a-long-random-string

# פרטי משתמש האדמין הראשוני (נוצר אוטומטית בהפעלה הראשונה)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ─────────────────────────────────────────────────────────────
# התראות על הזמנה חדשה — כל הערוצים אופציונליים.
# הגדירו רק את מה שאתם רוצים להפעיל; מה שלא מוגדר פשוט מדולג.
# ─────────────────────────────────────────────────────────────

# ── ערוץ 1: WhatsApp דרך CallMeBot (חינמי) ──
# הפעלה חד-פעמית לפי ההוראות ב-https://www.callmebot.com/blog/free-api-whatsapp-messages/
# WHATSAPP_PHONE=972541234567
# CALLMEBOT_APIKEY=123456

# ── ערוץ 2: אימייל דרך SMTP (למשל Gmail עם App Password) ──
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@gmail.com
# SMTP_PASS=your-app-password
# NOTIFY_EMAIL_TO=owner@example.com
# NOTIFY_EMAIL_FROM=you@gmail.com

# ── ערוץ 3: Webhook כללי (Zapier / Make / n8n → וואטסאפ, SMS, Slack...) ──
# NOTIFY_WEBHOOK_URL=https://hooks.example.com/your-webhook
